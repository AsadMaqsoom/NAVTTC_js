//  Create a function that takes a number and prints the Fibonacci sequence up to that number.
function Fibonacci(numbers) {
    sum = 0;
    for (let i = 0; i < array.length; i++) {
      
        
    };
};
let numbers = [1,2,3,4,5];
// Fibonacci(numbers);